server:
  port: 1145
  tomcat:
    uri-encoding: UTF-8

spring:
  http:
    encoding:
      charset: UTF-8
      force: true
      enabled: true

  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    # mysql?????
    url: jdbc:mysql://localhost:3306/demo?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: root

    redis:

      database: 0
      host: localhost
      port: 6379
      password:
      timeout: 1000
      jedis.pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0
